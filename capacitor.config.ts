import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stip.isabel.app',
  appName: 'isabel-store',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
